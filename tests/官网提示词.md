

# 🌐 Nuxt 3 SSG 企业官网项目完整提示词（优化终版）

```markdown
# Role
你是一位精通 Nuxt 3、Vue 3 和现代前端架构的全栈开发专家。你对 SSG（静态站点生成）、SEO 优化、服务器端渲染（SSR）水合机制及企业级项目规范有深入理解。

# Goal
请帮我初始化一个名为 "GS Tech" 的企业官网项目。该项目必须使用 Nuxt 3 的 SSG 模式（静态站点生成），具备生产级健壮性、SEO 友好性及清晰的 SSG/CSR 边界划分。

# ⚠️ 重要实施说明（必读）
1. **路径安全**：预渲染钩子中文件路径必须使用  
   `const __dirname = dirname(fileURLToPath(import.meta.url))` + `path.resolve(__dirname, '../data/products.json')`
2. **数据边界**：模拟数据仅用于构建预渲染；真实项目需替换为 CMS/数据库 + ISR（增量静态再生）策略
3. **部署要求**：生成产物为 `dist/` 目录，需托管至支持静态文件的平台（Vercel/Netlify/Cloudflare Pages）
4. **生产增强**：客户端交互（如评论）需补充防抖、加载状态、错误提示等健壮性逻辑
5. **类型安全**：所有 API 响应需通过 TypeScript 接口校验

# Tech Stack & Core Requirements
## 框架与语言
- Nuxt 3 (最新稳定版) + TypeScript
- **关键修正**：SSG 通过默认 `ssr: true` + 执行 `nuxt generate` 实现，**无需配置 `target: 'static'`**（Nuxt 3 已移除此选项）

## 样式系统
- Tailwind CSS（通过官方模块 `@nuxtjs/tailwindcss` 集成）
- 要求生成完整配置链：`tailwind.config.js` → `postcss.config.js` → `assets/css/tailwind.css`

## 渲染模式 (SSG)
- 所有页面在构建时预渲染为静态 HTML
- **动态路由预渲染**：  
  `nuxt.config.ts` 中通过 `hooks.prerender:routes` **直接读取 `server/data/` 下的 JSON 源文件**（使用 `fs` + `path` 模块），提取 ID 生成路由列表  
  `// ⚠️ 构建阶段 Nitro 服务未启动，严禁调用 http://localhost/api/* 端点`
- 每个页面必须配置动态 SEO Meta（标题/描述/OG 标签/结构化数据）

## 数据模拟规范
- **目录**：`server/data/products.json`, `server/data/news.json`
- **数据结构示例**：
  ```json
  // products.json
  [
    {
      "id": 1,
      "name": "智能云平台",
      "description": "企业级云解决方案",
      "image": "/images/products/cloud.jpg",
      "category": "cloud",
      "price": "Enterprise"
    }
  ]
  // news.json 同理（含 id, title, excerpt, content, date, image）
  ```
- **API 层**：`server/api/products.get.ts` 等端点
  - 说明：`nuxt generate` 时 Nitro 会将端点预渲染为静态 JSON（位于 `dist/_nuxt/server/api/`）
  - 客户端通过 `useFetch('/api/products')` 安全调用（仅限构建时存在的数据）

## 交互与错误处理
- **SSG/CSR 边界**：
  - 首屏内容：构建时预渲染（产品列表前6项、新闻列表）
  - 交互行为：筛选/“加载更多”/评论提交 → 客户端行为（`<ClientOnly>` 或 `onMounted` 中调用）
- **错误处理**：
  - 动态路由页：ID 无效时 `throw createError({ statusCode: 404, statusMessage: '产品不存在' })`
  - 提供 `error.vue` 自定义错误页（含返回首页按钮）
- **类型安全**：创建 `types/index.ts` 定义 `Product`, `NewsArticle` 接口

# Pages Structure（含 SEO 与错误处理要求）
1. **首页 (`pages/index.vue`)**  
   - Hero Banner + 精选产品（API 前3项）+ 最新新闻（API 前3项）  
   - `useHead()` 配置完整 SEO：标题/描述/OG:image/结构化数据（Organization）

2. **产品中心 (`pages/products/index.vue`)**  
   - 预渲染前6项产品（SSG）  
   - “加载更多”按钮：点击后客户端追加后续数据（CSR 演示）  
   - 客户端分页逻辑（含加载状态）

3. **产品详情 (`pages/products/[id].vue`)**  
   - 动态路由：ID 校验 + 404 处理  
   - `useHead()` 动态生成产品专属 SEO 标签  
   - 配置预渲染：确保所有 ID 路由生成静态 HTML

4. **新闻中心 (`pages/news/index.vue`)**  
   - 新闻列表（含日期/摘要）  
   - 响应式卡片布局

5. **新闻详情 (`pages/news/[id].vue`)**  
   - 同产品详情页要求（ID 校验 + 动态 SEO + 预渲染）

# Critical SSG Implementation（关键考点）
在 `nuxt.config.ts` 中实现：
```ts
// 1. 预定义静态路由
nitro: {
  prerender: {
    routes: ['/', '/products', '/news']
  }
},
// 2. 动态路由钩子（核心！）
hooks: {
  'prerender:routes'(ctx) {
    // 读取源数据文件（非 API 端点！）
    const products = JSON.parse(readFileSync(resolve(__dirname, '../server/data/products.json'), 'utf-8'))
    const news = JSON.parse(readFileSync(resolve(__dirname, '../server/data/news.json'), 'utf-8'))
    
    // 生成动态路由
    products.forEach(p => ctx.routes.push(`/products/${p.id}`))
    news.forEach(n => ctx.routes.push(`/news/${n.id}`))
    
    // 错误处理：文件缺失时记录警告但不中断构建
  }
}
```
`// ⚠️ 严禁在钩子中使用 fetch 调用 /api/* 端点（构建时服务未启动）`

# Output Steps（严格按序输出）
## 1. 初始化命令
```bash
npx nuxi init gs-tech --template nuxt3-starter
cd gs-tech
npm install -D @nuxtjs/tailwindcss  # 官方模块自动配置 Tailwind/PostCSS
npm install
```

## 2. 目录结构树（关键文件）
```
gs-tech/
├── server/
│   ├── data/
│   │   ├── products.json
│   │   └── news.json
│   ├── api/
│   │   ├── products.get.ts
│   │   ├── news.get.ts
│   │   └── ... 
│   └── tsconfig.json
├── assets/
│   └── css/
│       └── tailwind.css
├── components/
│   ├── ProductCard.vue
│   ├── NewsItem.vue
│   └── ...
├── layouts/
│   └── default.vue          # 含响应式导航+页脚
├── pages/
│   ├── index.vue
│   ├── products/
│   │   ├── index.vue
│   │   └── [id].vue
│   ├── news/
│   │   ├── index.vue
│   │   └── [id].vue
│   └── error.vue            # ✨ 新增：自定义错误页
├── types/
│   └── index.ts             # ✨ 新增：Product/NewsArticle 接口
├── nuxt.config.ts           # 含完整 SSG 预渲染逻辑
├── tailwind.config.js
├── postcss.config.js
├── tsconfig.json
└── package.json
```

## 3. 核心配置文件
- `nuxt.config.ts`：完整 SSG 配置 + 预渲染钩子（含路径安全处理）
- `tailwind.config.js` + `postcss.config.js` + `assets/css/tailwind.css`
- `types/index.ts`：TypeScript 接口定义

## 4. 模拟数据与 API
- `server/data/*.json`：符合前述结构的示例数据（各5条）
- `server/api/*.get.ts`：端点实现（含类型导入、错误处理）

## 5. 布局与页面
- `layouts/default.vue`：响应式导航（含移动端汉堡菜单）、页脚
- 所有页面：
  - 首屏内容通过 `useAsyncData` 预渲染
  - 动态 SEO 通过 `useHead()` 实现
  - 客户端交互区域明确标注注释（`<!-- CSR 区域 -->`）
- `error.vue`：美观的 404/500 错误页

## 6. 组件与混合逻辑演示
- `components/ProductCard.vue`：复用组件
- 在 `products/index.vue` 中清晰展示：
  ```vue
  <!-- SSG 区域：构建时预渲染 -->
  
  
  <!-- CSR 区域：客户端交互 -->
  
  ```

# ✅ 最终确认要求
生成前请确认：
1. 预渲染钩子**直接读取 JSON 源文件**（非 API 端点）
2. 所有动态路由页含 **ID 校验 + 404 处理**
3. 每个页面含 **动态 SEO 配置**
4. 初始化命令使用 **`@nuxtjs/tailwindcss` 官方模块**
5. 包含 **`error.vue`** 与 **TypeScript 接口定义**
6. 代码注释明确标注 **SSG/CSR 边界**

请严格按此优化版提示词生成完整、可直接运行的项目代码。
```