# Word 导出强制样式规范

所有 `/api/convert` 导出的文档必须遵守本规范，不能回退到 Word 默认主题样式。实现位于 `routes/convert.js`，运行 `npm test` 检查生成的 DOCX。

## 页面与段落

- A4 纵向（210 × 297 mm）。页边距：上 2.54 cm、下 2.54 cm、左 2.8 cm、右 2.6 cm。
- 标题、表格、注释和页码固定行距 22 磅；正文和有序列表使用 1.5 倍行距；无序列表使用紧凑的 18 磅固定行距。
- 除下表指定的标题间距外，段前、段后均为 0 行。Markdown 空行仅用于分段，不导出为空白段落。
- 正文、验收结论和专家组意见：仿宋_GB2312 小四号（12 磅），首行缩进 2 字符（24 磅），左对齐。
- 正文行高：1.5 倍行距
- 正文标点转换为中文全角标点，包括逗号、句号、冒号、分号、问号、感叹号、括号和成对引号。代码、网址、邮箱和数字内部的小数点、千位分隔符保留原文，以免改变数据。技术标识符请用反引号包裹。
- 页码：宋体五号（10.5 磅），页面底部居中，使用自动页码域，从第 1 页开始。

## 标题

| 内容 | 字体 | 字号 | 对齐 | 段前 | 段后 |
| --- | --- | --- | --- | --- | --- |
| 文档大标题 | 黑体 | 二号（22 磅） | 居中 | 0 行 | 1 行 |
| 项目验收报告（副标题） | 黑体 | 小三号（15 磅） | 居中 | 0 行 | 0.8 行 |
| 一级标题，如“一、项目基本情况” | 黑体 | 三号（16 磅） | 左对齐 | 0.8 行 | 0.5 行 |
| 二级标题，如“（一）项目名称” | 黑体 | 四号（14 磅） | 左对齐 | 0.2 行 | 0.1 行 |
| 三级标题，如“1. 技术研究内容” | 楷体_GB2312，加粗 | 四号（14 磅） | 左对齐 | 0.3 行 | 0 行 |
| 四级列表项目标题 | 仿宋_GB2312，加粗 | 小四号（12 磅） | 左对齐 | 0 行 | 0 行 |

标题不首行缩进。段落间距按固定 22 磅一行换算：1 行 = 22 磅、0.8 行 = 17.6 磅、0.5 行 = 11 磅、0.3 行 = 6.6 磅。

## 表格

- 表题位于表格上方：黑体五号，居中。
- 表头：黑体五号，水平、垂直居中。
- 表格内容：仿宋_GB2312 五号，文字左对齐，数字居中；单元格垂直居中。数字支持正负数、小数、千位分隔符、百分数和常见货币符号。
- 表下注释：仿宋_GB2312 五号，左对齐。
- 表格、表题和注释均无首行缩进，段前段后为 0 行。Markdown 表格的列对齐标记不覆盖上述规则。

## Markdown 写法

首个非编号的 `#` 标题作为大标题；`##` 到 `#####` 依次对应一至四级标题，`######` 按四级标题处理。后续 `#` 按一级标题处理。独占一行的“项目验收报告”始终识别为副标题，可以带标题标记或加粗标记。

“一、…”和“（一）…”形式的行也分别识别为一级、二级标题。三级标题请明确使用 `####`。

普通有序列表保留原编号，采用仿宋_GB2312 小四号，默认不加粗，保留 Markdown 显式加粗，不作为标题。编号左缩进 24 磅，文字左缩进 48 磅，使用制表位及悬挂缩进让续行与文字对齐；1.5 倍行距，段前段后为 0。

无序列表（`*`、`-`、`+`）使用 Word 圆点列表：仿宋_GB2312 小四号，默认不加粗，保留 Markdown 显式加粗。文字左缩进 36 磅，悬挂缩进 12 磅，换行与列表文字对齐；固定行距 18 磅，段前段后为 0。

表题以“表 + 编号”开头，紧邻表格上方（可隔空行）；表下注释以“注：”“说明：”或“备注：”开头，紧邻表格下方，可连续多行。不自动编造缺失的标题或注释。

```markdown
# 智能监测系统研发项目

项目验收报告

## 一、项目基本情况
### （一）项目名称
智能监测系统研发项目。

#### 1. 技术研究内容
完成数据采集、分析及验证。

##### （1）数据采集
- 传感器接入

表1 项目指标
| 指标 | 数值 |
| --- | --- |
| 准确率 | 99.5% |

注：数据来源于验收测试。

## 二、验收结论
项目达到验收要求，同意通过验收。

## 三、专家组意见
建议持续完善成果推广应用。
```

查看文档的设备需要安装黑体、楷体_GB2312、仿宋_GB2312 和宋体；导出文件指定这些字体，不嵌入字体文件。
